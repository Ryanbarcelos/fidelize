export interface LoyaltyCard {
  id: string;
  storeName: string;
  userName: string;
  cardNumber: string;
  points: number;
  storePin: string;
  logo?: string;
  createdAt: string;
  updatedAt: string;
}
